@using Microsoft.AspNetCore.Authentication
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    <script type="text/javascript" src="~/Scripts/jquery-3.4.1.js"></script>
</head>
<body>
    <h1 align="center">This is a Microservice Demo.</h1>
    @if (!User.Identity.IsAuthenticated)
    {
        <h3>Error! User Is not Authenticated.</h3>
    }
    if (User.Identity.IsAuthenticated)
    {
    <h2>HostWeb Claims</h2>
    <dl>
        @foreach (var claim in User.Claims)
        {
            <dt>@claim.Type</dt>
            <dd>@claim.Value</dd>
        }
    </dl>
    "API AccessToken: Bearer @ViewData["APIAccessToken"]
    <br />
    Current User Roles：@Html.DropDownList("ddlCurrentUserRoles")
    <br />
    Current User Permissions：
    <div id="divCurrentUserPermisson"></div>
    <br />

    <div>
        <h2>API Claims: <a href="/Home/GetApiClaims">GetApiClaims</a></h2>
        <dl>
            @foreach (var item in ViewBag.ApiClaims)
            {
                <dt>@item.Key</dt>
                <dd>@item.Value</dd>
            }
        </dl>
        <br />
        <a href="/Home/Logout">Logout</a>
        <br />

        }
    </div>

</body>

<script type="text/javascript">
    $(document).ready(init);

    function init() {
        $("select[name='ddlCurrentUserRoles']").change(function () {
            var selectedValue = $(this).val();
            ShowCurrentUserPermission(selectedValue);
        });
    }

    function ShowCurrentUserPermission(val) {
        $.ajax({
            url: "@Url.Action("ShowCurrentUserPermission", "Home")",
            data: { provinceId: province },
            success: function (data) {
                $("#divCurrentUserPermisson").html(data);
            }
        });
    }
    </script>
</html>
